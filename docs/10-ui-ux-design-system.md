# 10 — UI/UX & Design System

## 1. Design language

Clean, whitespace-heavy, card-based; the AI is the hero feature but nothing depends on it. Reference feel: *Linear's calm + Stripe Dashboard's data density, tuned for school administrators — friendly, not fintech-cold.*

### Tokens (source of truth)

| Token | Value | Use |
|---|---|---|
| Cyan-Teal | `#00a5cb` | Primary actions, primary chart series. **Meadow, 2026-09-03** (was `#008a9d`, and `#028090` before that) |
| Ink | `#0d2a31` | Headings, sidebar/nav, dark bands (lifted with the palette so the chrome is not a generation behind the charts) |
| Seafoam | `#4a913b` | Secondary accents (meadow's deep ramp step) |
| Meadow | `#72ba63` | Success, active/ON states, "collected" |
| Ochre | `#a17a00` | Warnings, fees-outstanding, third chart series. **The darkest colour in the set, and that is load-bearing** — green and this collapse to one hue under protanopia and deuteranopia, so only their lightness gap separates them |
| Rose | `#cd617e` | Errors, defaulter counts, FALSE branches |
| Violet | `#a886e5` | The fifth chart series. No fixed meaning |
| Slate `#334155` / Muted `#64748B` | Body text / captions |
| Canvas `#F1F5F9` / Card `#FFFFFF` | Page background / cards |

Type: **Inter**, one scale — Page title 28 semibold · Card heading 18 semibold · Body/table 14 · Caption/kicker 11 (+2 tracking); tabular figures for all numbers. Components: 8 px radius cards with soft shadows; primary (filled teal) / secondary (teal outline) / ghost buttons; chips; KPI cards (big number + delta pill); switches.

**Surfaces amended 2026-09-03**, alongside the chart-paint amendment in principle 2 below and for the same reason — the charts got quieter, and chrome that was merely *present* before became the loudest thing on the page once it no longer had gloss to compete with. Card radius **14 px** (from 12: at the 340–720 px a panel occupies, 12 reads as a square with its corners knocked off, and chips keep the 7 px `--radius-sm`, so "small control" and "surface" are now visibly different rather than two pixels apart). Card shadows roughly **halved** — a dashboard is cards on a tinted canvas and the canvas already separates them; at the old weights eleven panels read as a stack of floating slabs. `--shadow-float` is untouched: a popover really is floating. Panel padding 20/22 and grid gap **20 px** (from 16), because a quiet card takes its definition from its whitespace rather than its edges. A KPI figure is **29 px** (from 26) over an 11 px tracked kicker on a 3 px rhythm — the figure is the reason the tile exists and was sitting at barely twice its own label. A table's header is a **label strip, not a filled band**: sticky and card-coloured with a single rule under smaller, tracked, muted type, where it used to carry a grey fill that was the loudest horizontal element on a card whose subject is the rows below it; rows go to 12 px of vertical padding with a lighter divider, since a column of figures is scanned down and what makes that easy is air between lines, not rules.

**Surfaces amended again 2026-09-03 (third pass, "Meadow Lit").** Two of the numbers above had already been corrected in code without the doc following, and this records where they landed. Card radius is **12 px** — the second pass took 14 → 8 arguing that a rounder panel detaches from its neighbours and floats, which is right about the effect and wrong about the cause: what makes eleven panels float is the *shadow*, and that is where the argument now applies. Panel padding is **12/14** and the grid gap **12 px** (not 20): a reporting surface is read by comparing panels against each other, and comparison wants them close — the 1px borders do the separating. A KPI figure is **24 px**, because presence comes from contrast with its 11 px kicker rather than absolute size, and 29 px was setting the height of the strip that pushes every chart below the fold.

What is new in this pass is **tint**. Card shadows stay hairline-weight but gain a wide, very soft second layer, so a white card on a tinted canvas has form without lifting off it. The canvas itself carries three barely-there hue pools (`--grad-canvas`, all under 10% alpha) rather than being flat grey; a card falls about 1.5% toward the canvas from top to bottom; the sidebar gains a long vertical fall through the ink with the brand teal pooled behind the mark; and the topbar is **frosted** (82% white + blur) rather than opaque, because it is sticky and content scrolls under it. Every wash in the product is drawn at **135°** from one token set (`--grad-teal/mint/amber/rose/violet`, plus `--grad-ai`), built from the same `RAMP` steps `packages/chart-spec` paints marks with — one light source, one construction, so a KPI tile's accent and the bar summarising the same measure cannot drift apart. `--grad-ai` (teal → violet) is the one place two hues are deliberately crossed, and it is reserved for the AI surface, which reports no measure and therefore can carry no measure's colour.

### Principles

1. **Calm surfaces** — data is the color, chrome is quiet.
2. **One chart language** — every report, custom report, AI artifact and PDF draws through one renderer, so they cannot drift (a direct consequence of spec-driven rendering, doc 05). **Amended 2026-09-01.** Categorical series are **five** steps (`#008a9d`, `#3db575`, `#c78100`, `#c74859`, `#4967c6`), each with a three-step ramp (deep / base / lit) used to shade marks. The prior four-step set was capped at four because a 2026-08-22 audit found two teal-family steps indistinguishable at mark size, and §1.2's "teal-family series" rule ruled out reaching outside the family to fix the spacing. That rule is what is amended: **hue variety is now allowed beyond the teal family**, because leaving the family is exactly what buys the separation the old audit could not find inside it — indigo is nowhere near teal, where a second teal step never could be. The slot ORDER remains semantic and is not permutable (demand, collected, pending, defaulters), so hues are pushed apart within their families and lightness carries the separation that hue cannot: under protanopia and deuteranopia green and amber collapse to one hue, and only their lightness gap survives. Measured on the shipped set (`scripts/validate_palette.js`): worst adjacent pair ΔE 9.1 deutan / 9.8 tritan, worst normal-vision pair ΔE 16.2, against floors of 8 and 15; all five inside the lightness band and above the chroma floor, which the old set failed on two slots and one slot respectively. One accepted deviation, stated rather than hidden: `#3db575` sits at 2.54:1 against the card, under the 3:1 relief line — the remedy is visible labels or a table view, and every multi-series chart here carries a mandatory legend, axis values and a tooltip. The old palette failed that check on **two** slots. A category past the fifth is still never a generated hue; it folds into a recessive "Other" fill (`#64748b` at reduced opacity), alternating with `#cbd5e1` inside a stack.

    **Amended 2026-09-03 (second) — the palette itself is now "Meadow", and marks are FLAT.** Chosen from four directions put side by side on real Fee Collection figures. The brief was softer, less dark colours; the finding that shaped the answer is that **a five-slot categorical palette cannot simply be washed out to get there.** Four hand-picked pastel sets were measured and all four failed: draining chroma dropped them below the 0.10 floor at which a colour starts reading grey, and it collapsed teal↔green to ΔE 10 against a normal-vision floor of 15 — a pair *full-colour* readers cannot separate. So softness is bought with **lightness and with how the mark is drawn**, never by draining hue. Measured on the shipped set (`scripts/validate_palette.js`, surface `#ffffff`): worst adjacent CVD pair ΔE 10.4 deutan, worst normal-vision pair ΔE 16.3, all five inside the lightness band and above the chroma floor — every check passes. One accepted deviation, unchanged in kind from before: `#72ba63` sits at 2.35:1 against the card, under the 3:1 relief line, and the remedy is the relief this product already ships — a mandatory legend on every multi-series chart, axis values, and a table view. The old palette failed that check on two slots.

    Three treatment rules come with it, and they replace the shading rules amended earlier the same day:

    - **A mark is one colour — the one in the legend.** *(Superseded by the third amendment below, which restores the wash with the legend swatch washed to match; the rule's REASON — a mark must be matchable against its key — is what the replacement satisfies.)* Bars carry no gradient at all now. They have been through three treatments and the reasoning ran out in a useful direction: a five-stop cylinder read as plumbing, and a two-stop sheen still meant a reader matching a bar to its legend dot was matching a colour to the *midpoint of a wash*. Donut slices are flat for the same reason. There is no `<defs>` on a bar chart any more.
    - **The tonal interest moves behind the mark, into a rail.** Every bar sits in its own hue at **10%**, spanning the full scale (`RAIL_STRENGTH`). This is not the grey track removed on 2026-09-01: fourteen grey rails behind fourteen teal bars read as a striped table with the data faint inside it, the scale louder than the measurement. A rail *in the hue* reads instead as the unfilled part of the same measure, so a short bar is a small share of something rather than a stub floating in space. Never on a **stacked** bar, whose segments are parts of one bar — a full-scale rail behind each would draw four scales for one measurement.
    - **The rails replace the grid on bar charts.** A rail per mark and a rule per tick are two scales drawn over each other. Line charts keep their dashed grid, because nothing else there says where the scale runs.

    The `toneColour` map that paints a KPI figure now reads its four values **out of `SERIES` by slot**. It had drifted two palette generations behind and was painting a tile and the chart summarising the same measure in visibly different colours; reading it from the source means the next palette change cannot leave it behind again.

    **Amended 2026-09-03 — how a mark is PAINTED, which is a separate question from which hue it takes.** The palette above is unchanged; every hue, its slot and its semantics stand. What changed is the treatment, after a review against a reference dashboard whose charts read markedly cleaner than ours at the same data density. Four rules now bind, all in `packages/chart-spec/src/react/widgets.tsx`:

    - **A mark is its hue, not a rendering of a solid.** A bar's fill is a **two-stop** gradient across its thickness — the ramp's new half-step `soft` at the lit edge, `base` at the far edge — and a donut slice takes the same two stops. It was a five-stop cylinder (deep → base → lit → base → deep) plus a hue-coloured drop shadow, and that read exactly as designed: as tubes. The whole of a bar's meaning is where its end lands, and shading that models a *surface* spends the reader's attention on the middle of the mark; three tubes side by side in a grouped chart also fought each other, and fourteen of them read as plumbing. The two-stop version keeps the one thing the shading bought — a lit edge, so a mark sits on the card rather than being a hole cut in it — and drops the rest. It also fixes a real defect: a five-stop wash ending on `deep` meant a reader matching a bar against its legend dot was matching two different colours. The `deep` and `lit` steps remain, for pressed states, KPI tile edges and the donut. The ramp gains **`soft`** (`#2f9eb0`, `#5fc98e`, `#d99833`, `#d96471`, `#607ed6`) because painting a 22px bar from `lit` made it visibly two-tone.
    - **A single-series bar chart shades its bars along the series' own ramp by magnitude**, `soft` at the bottom of the range to `deep` at the top, measured from zero exactly as the bar length is. This is *not* the "paint the tallest bar darker" device removed on 2026-09-01: that singled out one mark on a single-measure chart and therefore read as a second series. A continuous ramp picks out nothing, groups nothing, and is a function of the same number the length already encodes — redundant with it, which is what makes it safe. It exists because fourteen classes in one flat teal is a picket fence with no focus, and single-series bar charts are what this product has most of.
    - **Chrome is subtracted from every chart.** Grid lines are dashed (`3 4`) and lightened to `#e3e8ef` — a grid is a ruler, and a solid rule inside a card that already has a border reads as a second box. Axis lines and tick stubs are gone entirely: the card is the frame and the grid is the scale, so a third boundary says nothing new. The per-bar background "track" is gone — on three schools it framed the marks, on fourteen classes it drew fourteen full-width grey rails behind the data and the panel read as a striped table. A line chart's area fill drops from 0.30 to 0.18, past which the eye starts reading the region's *area*, a quantity nobody measured.
    - **Legends and points read as a key, not as decoration.** A multi-series legend is centred over the plot rather than left-aligned under the title, where it read as a subtitle with dots in it. Line points are **rings** — card-coloured inside the series stroke — with the latest point the single filled disc on the chart; filled dots everywhere beaded the line and made "latest" a 1.5px radius difference nobody can see. A donut's legend is a two-column key carrying each slice's **share**, because a donut's subject is proportion and a legend of bare names hands the reader a colour key and asks them to eyeball angles. A percentage, never the raw value: the figures reach the renderer as numbers rather than the server-formatted strings a KPI carries, and a ratio needs no locale.

    **Amended 2026-09-03 (third) — "Meadow Lit": the wash comes back to the marks, and the matching problem is answered rather than avoided.** The palette is unchanged again — every hue, every slot, every semantic, and every measured margin above still stands; `scripts/validate_palette.js` is untouched and still passes, because nothing here introduces a hue. What is amended is the *flat marks* rule of the second amendment, on the finding that it over-corrected: a page of flat fills on flat rails reads as bland at exactly the data density this product runs at, which is the complaint it came back as.

    Marks are washed again, and the objection that removed the wash twice — that a reader matching a bar to its legend dot is matching a colour to the *midpoint of a wash* — is now fixed instead of dodged. Three properties make the wash safe where the five-stop cylinder and the two-stop sheen were not:

    - **Every stop is a lightness step of ONE audited hue** (`RAMP`), so hue and chroma are held across the whole mark and every CVD separation measured above is a separation between marks, not within one.
    - **The base sits at the 52% stop**, so the midpoint of the mark — the part the eye samples — is exactly the colour named in `SERIES`.
    - **The legend swatch is painted from the same stops** (`legendDotStyle`, sharing `markStops` with the SVG gradient so the two cannot be tuned apart). A reader matches a wash to a wash. This is the piece the two earlier attempts never had, and it is the whole of why this one binds.

    The lit end deliberately stops **six tenths of the way** to the ramp's `light` step: `light` is a cap sized for a donut wedge, which has area and a card-coloured hairline around it, and a 38px column painted in it dissolves into the card. The wash always runs **axis → tip**, so it reinforces the direction the length is read in. Rails, the magnitude ramp, the dashed grid, the centred legend and the ring/disc line points are all unchanged — this amendment touches paint, not structure.

    Two additions come with it:

    - **A trend line carries a soft drop shadow in its own hue** (`LineGlow`, `feDropShadow` at 0.28) and a stroke washed along the trend. A line is one 3px stroke on a white card, which is why a chart made only of lines reads thin however saturated it is. `print.css`'s `svg [filter]` guard strips this on paper, where a hue-coloured blur separates into a CMYK smudge — the guard was kept after the 2026-09-01 shadow was removed precisely so the next filter would be covered before it shipped, and it was. **Gradients are not stripped on paper**: a `linearGradient` is paint, not an effect, and removing it would make the export disagree with the page it was taken from (ADR-021).
    - **A KPI tile whose value is a bare percentage draws a dial** (`MeterRing`) — an arc of the metric's own hue on a rail of that hue, rounded cap, gradient sweep. The share is read back out of the **server's own formatted value** (`rateShare`, strict `^\d+(\.\d+)?%$`); there is no second numeric field on the widget and asking the server for one would be a second representation of a number it already sent, so the dial and the digits are the same value drawn twice and cannot disagree. Anything that is not a bare percentage gets no dial and the tile is laid out exactly as before. Attendance at 86% beside realisation at 53% is legible before either figure is read, which is the argument for it.

    **Amended 2026-09-03 (fourth) — the ACCENT set widens from four to nine. `SERIES` does not move.**

    Keep the two apart, because they answer different questions and only one of them is audited. `SERIES` orders five hues **semantically** (demand, collected, pending, defaulters, fifth) and its constraint is the separation between *adjacent marks inside one chart* — the hard one, since deuteranopia collapses green and ochre to a single hue and only their lightness gap survives. An **accent** colours a chart that has *one* series: there is nothing inside it to be confused with, and the only separation that matters is between one card and the card next to it. That is a far weaker requirement, and it is met by spreading hues around the wheel.

    The four accents were `primary` / `secondary` / `warning` / `negative`, and the last two were reserved for the cases where their meaning actually applied — so most preview cards fell back to `primary` and the Dashboard read as one blue page. A grid whose entire job is to hold eight different subjects side by side was painting six of them the same colour, which is the one thing that makes a set of cards unscannable. Five identity-only hues are added: **`violet` `#a886e5`** (the fifth series), **`indigo` `#5f8fe0`**, **`aqua` `#2fb3aa`**, **`coral` `#e8896b`**, **`plum` `#bd77d2`**. Each gets a `RAMP` entry built exactly as the series ramps are — lightness moved, hue and chroma held — so an accent and a series mark are painted by identical machinery. The lead stays teal and the new hues are led by the blues (`indigo` and `aqua` sit either side of teal), so the page still reads as one product with warm and purple counterweights rather than as a paint chart.

    **`warning` is the one accent that differs from its series slot,** and the reason is the CVD constraint itself. `SERIES[2]` is `#a17a00` and is that dark *on purpose*: it is the only thing separating "pending" from "collected" for a red-green colour-blind reader looking at a grouped bar. A single-series chart has no green beside it, so the lightness gap buys nothing there and the cost is real — Fee Defaulters' ageing chart rendered as four brown bars. The accent is therefore the lighter **`#d09a2c`**, applied only where the constraint does not hold; `SERIES[2]` is untouched, so every grouped bar in the product is exactly as it was. At 2.4:1 against the card it sits under the 3:1 line — the same accepted deviation `#72ba63` already carries, with the same relief (axis values, a title, a wash that deepens toward the axis). Its ramp's `deep` step is held high (`#b8862a`) because amber loses chroma into brown faster than any other hue here, and the magnitude ramp paints the tallest bar in `deep`.

    **Meaning still outranks arrangement, and that is what makes any of this safe.** `measureColour` lets a widget's own `tone` win over an accent, so a caller cycling hues across a grid can never repaint overdue money in the teal it used for headcounts. The same precedence now applies to a **KPI tile** (`kpiColour`): a strip may colour a tile by subject, but only where `tone` is `neutral` — which is not a judgement the server made, it is the absence of one (services/home.ts marks every Dashboard tile neutral precisely because no threshold makes a student count good or bad news). A tile the server marked `warning` or `negative` keeps the colour its meaning earned, whatever position it sits in. The Dashboard strip cycles `primary · indigo · aqua · violet · coral` **by position, not by metric**: a hue chosen per metric would be a second, client-side claim about what that metric means, and the browser is the one place this product refuses to put meaning.

    **Vertical category axes tilt only when they must (2026-09-03).** A label of six characters or fewer is drawn flat; longer ones keep the −35° tilt. Every such axis was tilted unconditionally, which is right for twelve month names in a half-width panel and pure chrome for four quarters, six academic years or twelve roman-numeral classes — it makes a reader tilt their head to read "Q1" and gives the panel a ragged bottom edge. Six characters is ~44px, which fits at every count and panel width the grid produces; the test is deliberately conservative, because this module does not know the panel's width (see `rotatedTicks`), so no axis that reads today gets worse.
3. **Tenant theming** — each school/trust gets accent color + logo (topbar, buttons, PDF header) from the same token system; no per-client CSS forks.
4. **Feels instant** — skeleton loaders, streamed AI widgets, optimistic filter changes; UI responds within 100 ms even while data loads.

### 1.5 The Dashboard: the "AI Dashboard" artifact, card for card (adopted 2026-09-04)

The Dashboard, and the shell every screen sits in, follow the signed-off **"AI Dashboard" artifact** (Claude artifact `f5844b8a`, a design reference like the prototype in docs/11) **card for card, in each of its three layouts**. The product owner's rule, recorded here because it binds later work: *same layout, same graph* — a card is not moved, merged or replaced, and the data is fitted to the artifact's chart rather than the other way round.

**Three layouts**, picked in a control bar above the Dashboard and remembered per browser (`theme/dashboardTheme.ts`), each fetching only its own cards from `GET /api/home/overview/:slot` (`services/overview.ts`, backed by the vetted `dashboard-overview` report in the MCP catalog). A fourth tab, **My View**, sits to the left of them and is the reader's own board rather than one of the artifact's layouts — see §1.6:

| Artifact card | Format A · Gradient nav | Format B · Clean grid | Format C · Sidebar frame |
|---|---|---|---|
| Buyer Location (map) | **Six coloured tiles** (the owner replaced the map): students, staff, today's student attendance %, today's staff attendance %, new admissions this year, fee defaulter amount. **Every tile carries its parts beneath its figure (amended 2026-09-07).** Staff was the last one standing on a bare number while its five neighbours each showed a split, which reads as a tile whose detail failed to load rather than as one with no detail to give. It now carries the same permanent / not permanent / unclassified split the KPI strip prints, from the same classification — `services/staff-types.ts`, the one place that decides what `stafftype` means, so the two screens cannot disagree about what "permanent" counts. Where a school spells that column entirely in the ERP's opaque codes the tile shows the headcount alone: three parts reading 0 / 0 / everything reports the column's shape, not the school's staffing. The statement gained a `GROUP BY`, not a second scan (Invariant 1) | — | — |
| Revenue Split (rings + bars + figure) | Student attendance %, fee realisation %, staff attendance %; figure = total fees billed | — | — |
| Monthly Revenue | Fee receipts by month | — | — |
| Weekly Sales / Weekly Orders | Receipts by week / student attendance % by week | — | — |
| Top Clients | Schools ranked by fee collected | — | — |
| Task Categories + Data Activity | Fee position donut (received · late fee · transport fee · pending) + the four by month | — | — |
| Total Works / Statistics / Daily Tasks | — | Billed and collected by year (two lines) / students on roll by year / attendance recorded | — |
| Team Members | — | Four students with the highest attendance, above a floor read off the school's own register (half the best-covered student's marked days, capped at 20, never below 5 — so a school four weeks into a term still ranks) | — |
| Four KPI gauges | — | Students (male/female), today's attendance (present/absent), new enrolment, staff attendance today | — |
| Latest Projects table | — | Students paying late or not paying, with a **Remind** action that drafts a mail (no recipient — the extract holds no parent contact; agreed 2026-09-04) | — |
| Inbox | — | Top 10 students by pending fees | — |
| Profile / Data Company tiles | — | — | User card / students, staff, new admissions (no download button, per the owner) |
| Area with legend toggles | — | — | Receipts · staff present days · attendance % by month |
| Project Company donut / Data Graphic gauges / Customer Analytics | — | — | Payment modes / fee realisation and today's attendance / students paying late by week |

**Definitions agreed with the product owner (2026-09-04).** *Today* is the latest day a register was marked on or before the as-of date, and the card names the day. *Transport fee* is the heads `Transport Fee`, `Transport Fees`, `Bus Fee`, `TPT1/2/3 FEE`, `TR.C`; *late fee* is the head `Late Fee`. A *late payer* has two or more receipts dated after the instalment they settle ended, or an overdue balance. One deviation from the plan as first stated, and why: "Total Works" was to plot fee collected against students on roll, but the two are different units on one axis and the roll would flat-line beside crores; it plots **billed against collected** by year and the roll takes the "Statistics" bars beside it.

**What every layout keeps.** A **theme colour** (seven swatches plus a free colour, derived into six slots by `derivePalette`, applied as `--c0`…`--c5` and through `ChartPaletteProvider`), a **chart type menu on every chart** (bar, horizontal bar, line, area, donut, pie, polar area, radar — presentation state only, never written to a spec, a saved report or a PDF), the scope picker and academic-year control in the chrome (§3 "scope is always on screen"), and Ask AI in the nav, locked with a path where no key is set. The artifact carries no Ask bar and no catalogue strip, so the Dashboard draws neither; §2's Home description is amended accordingly. Notices for dropped scope and partial data stay — they appear only when something is wrong.

### 1.6 The per-chart menu, and My View (adopted 2026-09-08)

**Every chart in the product carries the same "⋮" menu**, in the head of its card or panel — on the Dashboard in all its layouts, on the two pinned reports (Trend Analysis, Comparative Analysis), on every report inside Module Wise Analysis and on that screen's preview cards, on a custom report in the editor, and on My View itself. Five actions, always in this order and always all five listed (`components/ChartMenu.tsx`):

| Action | What it does |
|---|---|
| **Insights** (added 2026-09-08) | The AI explaining this chart in plain words — a headline, two to five short observations, and a caveat where the figures do not cover something. First in the menu because it answers the question a reader has before any of the other four: *what am I looking at.* See §1.7. |
| **Clone and customise** | docs/06 §3's clone-to-edit, per chart. A widget the server will build on its own (`CLONEABLE_WIDGETS`, mirroring `WIDGET_QUERY_KEYS` in `services/dashboards.ts`) offers a choice — **this chart** or **the whole report** — and any other panel offers only the whole report, because a panel that reads three result sets together cannot be cut out of its report without silently losing two of them. The whole-report clone carries the filter values the screen is showing (as-of, comparison year), exactly as the page button it replaced did. Opens a small form — what to copy, name, academic year, and a group-by where the widget takes one — and lands the reader in the editor on the copy. |
| **Add to My View** | Puts the chart on the reader's own board (§1.6 below). Reads **Remove from My View** once it is there. |
| **Enlarge** | The same chart, redrawn large over the page. A card the platform composes out of several widgets (the concentric rings, a gauge, the two tables) enlarges its OWN body rather than a substitute chart of the same numbers. |
| **View logic** | Invariant 6 per chart: source, scope, filters, group-by, served-from tier, the card's notes, and the read-only SQL — the report's own statements with the one feeding this panel marked, or the slot's statements for a Dashboard card. A surface that holds a chart without its report (a module preview card) fetches the statements when the reader asks, rather than on every card of every module page. |

**These two affordances are no longer page-level (amended 2026-09-08).** The report page's own "🧠 View logic" and "⧉ Clone & customise" chips are gone, and with them the page-wide logic panel they toggled. Both were answering a coarser version of the question a reader actually has — one slab of SQL for a report of eight charts, one clone of the whole thing — and keeping them beside the per-chart menu would have been two routes to the same place with different scope. CODING_GUIDELINES §17 is amended to match: all four standard affordances must still be REACHABLE from every report surface, and two of them are now reached per chart. What stays on the page is what is genuinely page-level: **⬇ PDF**, the scope and as-of line, the report's own filters (Compare with), and ✦ Ask AI about this data. The whole-report clone did not go with the button — it is the second option in any chart's clone dialog, which is what keeps it reachable on Fee Collection, whose four panels are each individually cloneable and would otherwise have left no route to copying the report.

**Nothing in the menu is ever hidden** — docs/10 §3's "locked ≠ hidden", one level down. An action a particular chart cannot offer (Clone, on a Dashboard card with no report behind it) is listed greyed with the reason on it, because a menu that grows and shrinks per card is a menu a reader has to re-read on every chart. The menu is page chrome and not spec: nothing in it is read out of a chart-spec or written into one (ADR-015), and the PDF surface, which renders no interactive controls, draws none of it.

**My View** is the fourth tab of the Dashboard's layout bar, left of View 1, and is empty until a reader puts something on it. What is stored is a **reference** — which Dashboard slot or which report, which widget id, and a title to label it with while it loads — never a figure, a row or a statement (`myView.ts`). The board then fetches exactly as every other screen does, under the scope and academic year in force now, so a reader who narrows their school selection watches their own board narrow with it; a saved figure would be a figure that outlived the scope check that produced it (Invariant 2). It lives in `localStorage`, like the layout and theme choice and for the same reason: it is the reader's arrangement of screens they already have permission for, and a per-user server table for a list of widget ids would put a write on the request path of a product whose point is that it never writes (Invariant 3). A storage that refuses yields an empty board.

A Dashboard card on the board is redrawn by **its own component**, from a table keyed by slot and widget id (`components/overview/registry.tsx`), not from its widget: half these cards are arrangements of several widgets, and redrawing "Attendance and fee realisation" as one donut would put a chart on the board the reader never saved. Report panels are fetched whole and once each, so a board holding four panels of Trend Analysis is one request. A saved card this build no longer draws says so in its place and offers to be removed, rather than rendering an empty frame. Order and removal are the reader's, through controls above each card — deliberately not a fifth item in the "⋮" menu, which is the same four actions on every chart in the product.

**Not covered:** an Ask AI answer that has not been saved. It has no stable id to reference and no report behind it, so it carries no menu; saving it to My Reports makes it a custom report, which does.

### 1.7 Insights: the AI explaining one chart (adopted 2026-09-08)

The first item of every chart's "⋮" menu. A reader who cannot tell what a chart is saying opens it and gets three or four plain sentences about **that chart**, written from **that chart's own figures**.

**It is not Ask AI with a canned question, and the difference is the whole design.** Ask AI plans tool calls, authors SQL through the MCP server and hydrates a new chart. Insights asks the database nothing at all: the reader is already looking at a chart the platform built, so `services/ai-insights.ts` rebuilds *that* chart through the same cached serving path the screen used (`buildDashboard` / `buildOverviewSlot` / `viewReport`), reduces it to a digest, and spends exactly one model turn. Three consequences, all of them load-bearing:

- **No new query.** The chart is in Redis under the key the screen warmed a moment ago, so an insight costs a cache read and a model call — never a replica scan (Invariant 1). It also cannot quote a number the chart does not show, because it is reading the built widget rather than re-deriving one.
- **No model-authored SQL.** There is no tool loop and no MCP call on this path, so the read-only data plane (Invariant 3) is not merely enforced here — it is unreachable from here.
- **No rows to the provider.** ADR-030 unchanged, and using that ADR's own stated mechanism: its trade-off section says a narrative is written from "aggregates/summary values the orchestrator can safely pass back (count, sum, min/max)", and that is literally what the digest is. Series become count, total, highest, lowest, first and last; a donut becomes its slices' shares; a KPI becomes the pre-formatted figure the tile already prints. **A table contributes its column names, its row count and per-column numeric aggregates, and never a cell.** That asymmetry is the privacy posture, not an omission: in this catalog the per-person data — defaulters, students, staff, late payers — is always a *table*, while the categorical axis of a bar, line or donut is always an institutional dimension (school, class, month, year, status, mode, department, age band). A label the masking pipeline already replaced travels as `[masked]` and the model is told not to speculate about it.

**A card that draws several widgets is explained as the card.** Half the Dashboard's cards are arrangements — the concentric rings are three rings plus a total, the Data Graphic reads the rings slot *and* the tiles slot, Fee position is a donut plus four sparklines — so the request names the widgets a card draws, each with its slot, rather than one widget id. A card drawing one widget names one part, which is the same request with no special case.

**What the model is told to do**, and it is as much of the feature as the digest: plain words, short sentences, no jargon; use only the figures given and quote them exactly as formatted (₹, Cr, L, %) so the words cannot disagree with the chart beside them; say what changed and where the extremes are rather than reading the values back; **never guess why** — "December collections are the highest of the year" is the model's to say, "because of the fee deadline" is not; and where the digest reports a thin base or partial data, say so in the caveat instead of writing confident sentences over it.

**BYOK, and said so on screen.** Insights runs on the organisation's own key (Invariant 5) and `/api/ai/insights` re-checks `ai_status` on every request. Where AI is not set up the item is **not greyed** — alone among the five — because it is the one whose lock has a fix: it opens and offers "Open Settings →" with a line saying the other four work without it, which is docs/10 §3's "locked ≠ hidden" doing its actual job. Every answer carries a line naming the model that wrote it, saying it reads totals and extremes and never individual records, and pointing at ⋮ → View logic for the SQL underneath — a reader deciding something on these sentences is entitled to know where they came from.

**Cost.** Answers are cached for an hour under the chart, the scope, the year and the permission class, and coalesced in flight, so two people opening the same card is one model call and not two charges on the school's own account. Longer than the chart's own ten-minute TTL on purpose: the numbers move as receipts land, but the sentence about them does not.

**What the PDF gets.** `print.tsx` mounts no palette provider and no `.skin` wrapper, so exports are still drawn in the Meadow palette of §1 by the unchanged `widgets.tsx` path (ADR-021). The Meadow tokens remain the audited, CVD-validated set; the skins are a layer over them inside the same `packages/chart-spec` renderer — still one chart layer. A derived palette is spread around the wheel, not CVD-audited; every multi-series chart carries a legend and every donut a keyed share list. Dark mode from the reference is not carried.

## 2. Screen inventory

| Screen | Essentials |
|---|---|
| **(ERP) menu → launch** | "📊 Analytics" item; handoff interstitial may show token-verification steps; there is **no login screen** in this product |
| **Main shell** | Sidebar (Dashboard, Dashboards, Attendance…, ⚡ Agents, 🤖 Ask AI, My Reports, Export History, ⚙ Settings); topbar: crumb · **school picker** · AY selector · avatar. **The AY selector became a real control on 2026-09-03**; it had been a read-only chip since the shell was built. Its options are the years the CURRENT SELECTION holds data for, newest first (`academic_years` on `/api/home`) — derived from rows the KPI strip was already reading, so it costs no extra query against a school database (Invariant 1). Never a fixed list: this product serves 1,500+ schools that went live in different years, and there is no window of years that is right for all of them. The list is the **union** of the metrics that answered, while the year it OPENS on still prefers the student roll — a school that has raised next year's fee demand before rolling its roll over has fee data for a year the roll has never heard of, and offering only the roll's years made that money unreachable from the one control that exists to reach it (which is the state the development extract was in when this was reported). Choosing a year rebuilds **both** the KPI strip and the preview cards: the cards already took the year as a parameter, so a strip left on the derived year would have put next year's charts under last year's totals with nothing on screen saying they differed. A metric with no rows for the chosen year renders as a `blocked_metrics` tile naming that year — never as `0`, which is the success-shaped failure of CODING_GUIDELINES §10 and would tell a reader their school has no children. A selection with one year keeps the plain chip: a select with a single option is an affordance that does nothing. **A total never claims schools that are not in it (added 2026-09-03).** The academic year is one value for the whole selection, resolved as the newest year ANY school in it has — and schools roll their student roll over at different times, so a trust routinely has one school already in the new year and two still in the old. The strip resolved to the new year, summed the one school that had it, and printed "Students · 3 schools — 1,760" while 4,801 children in the other two contributed nothing: no query refused, no school unreachable, no value null, and the number wrong by a factor of four in the same typeface as the true ones. So a metric that covers only part of the selection now says so twice — the tile's own label reads **"Students · 1 of 3 schools"** (the tile is what gets screenshotted and quoted, and a caveat above it does not travel with it), and a notice above the strip **names** the schools that are missing (`partial_metrics` on `/api/home`; names, not ids, because the reader is a director asking which of their schools is in the figure). Annotated, never silently reduced — the same answer ADR-011 gives for a school that could not be reached, and the reasons the alternatives were rejected: falling back to the newest year they ALL share hides the new year from the school that is up to date, and summing each school's own latest year adds 2026-27 to 2024-25 and calls the result a year. The two notices are worded differently on purpose: a degraded school FAILED, where these answered fine and simply have nothing recorded for that year, and "could not be reached" would send someone hunting an outage that is not there. This is invisible on any extract whose schools were rolled over together, which is why it survived to be found on a development one. **Amended 2026-09-01:** the per-report rows are gone. The menu is Dashboard · Trend Analysis · Comparative Analysis · 🧩 Module Wise Analysis · 🤖 Ask AI · 💾 My Reports · ⚙ Settings. Every predefined report had a row of its own, which is the list this menu was designed around at four reports and had become an inventory at twelve — sorted by nothing a reader cares about, and growing by one each time a dashboard was built. They now live one level down, grouped by subject (Module Wise Analysis row, below); nothing became unreachable, since a report opens from its module, from a Dashboard grid card and from the "More dashboards" strip. **Trend Analysis and Comparative Analysis keep rows of their own** — both are cross-cutting reads a Director opens as a habit rather than as "the fee report I want today", and filing a routine destination behind a category costs more than the row does. They remain inside their modules too: a shortcut, not a second copy. Both rows are read out of the served catalog **by id** (`PINNED`, `Sidebar.tsx`), so a report renamed in the catalog renames itself here and one withheld drops out without leaving a row that goes nowhere. |
| **Dashboard — what is on the first fold (added 2026-09-03)** | The KPI strip is **five** tiles: Students, Staff on roll, Student attendance, Total fees, and **Fee realisation** (collected ÷ demand). The fifth sits immediately after the fee tile because it is that tile's two halves as a rate — a reader meets the fraction before the quotient — and it exists because ₹48.8Cr against ₹92.0Cr is 53% and nobody sees that at a glance. Computed from **summed totals, never an average of per-school rates**, which would weight a 200-pupil school like a 4,000-pupil one; it is the same measure Fee Collection leads with, computed the same way, so the two screens cannot disagree. `tone: neutral` — a threshold for "good realisation" varies by school, by month and by how much of the year has elapsed, and a tile that turned amber below a number invented here would assert a standard nobody set. It is a regrouping of rows `/api/home` already reads, so it costs no query against a school database — which matters most here, on the one screen every user of every school loads (Invariant 1). **A "Where to look first" outliers table shipped beside it on 2026-09-03 and was removed the same day**: the comparison it drew belongs on Comparative Analysis, which already carries it, and Home's job is the summary and the way in to each report rather than a third place to read the same ranking. |
| **Dashboard — page furniture (added 2026-09-03, third UI pass)** | The greeting and the scope/as-of line sit on **one row** (`.pageHead`), not stacked: neither is a finding, and stacked they spent ~60px of the first fold — which on a reporting surface belongs to the numbers — on a name and a timestamp. The scope line remains mandatory and on screen per §3; it moved, it did not go. The **ask-bar is slim** (26px glyph, 8/12 padding). It was made *flat* in the third UI pass and lit again in "Meadow Lit": the height stays, but the one surface on the page that is **not** a reading of school data had ended up looking like the quietest thing on it. Colour returns only where it identifies — a `--grad-ai` wash on the glyph tile and the button, a ~7% tint across the bar, and a glow in the AI hue beneath it. The **KPI strip sits inside a labelled group** — a `<section>` headed "Key indicators", one step down from the card colour with a 1px border and no shadow. This is hierarchy, not decoration: a row of four cards floating on the canvas is four unrelated facts, and the same row under a heading is one answer to one question — it also gives a `blocked_metrics` tile a home, so "Students · NO DATA" reads as part of a set rather than as a card that failed to load. The group is **sunken rather than white** so the tiles inside read as tiles rather than as numbers floating in a box, and it is tinted with the canvas's own hues rather than flat grey. (The original reason was that a KPI tile painted only its 3px tone edge, the other three sides being zeroed by the `border-image` drawing it. That edge is a **pseudo-element** since "Meadow Lit" — `border-image` is not clipped by `border-radius`, so at a 12px corner it gave every tile two small square ears — and the tiles carry a full hairline again. The sunken well is still what separates a container from its contents.) A KPI tile also carries a **pool of its own tone** in the corner the figure does not occupy, under a tenth opacity: enough that five tiles read as five measures at a glance, nowhere near enough to tint the digits sitting on it. A preview card that has only an explanation to give **centres that text** rather than pinning it to the top of a card stretched to its row-mate: text at the top with 200px of blank under it reads as a card that failed to finish loading. |
| **School picker** | Multi-select with Select-all / Only-this-school; hidden for single-school users; every page, chat message and PDF carries the selection |
| **Dashboard / overview** | Greeting; hero **ask-bar** (locked state: disabled + 🔒 + "complete AI setup in Settings"; admins get "Set up now →", others "ask your administrator"); KPI strip; **"Your dashboards"** — one live preview card per `available` dashboard (its own lead widget, same cache entry the dashboard itself uses), catalog order (Fee Collection, Fee Defaulters, Staff Overview, Admissions Funnel, Attendance Analytics, Enrollment Overview), skeleton per card until its preview resolves; **"More dashboards"** — a single slim chip strip for built dashboards the curated grid does not lead with, not full tiles; every chip opens (`coming`/`blocked` cards are not served at all since 2026-09-01, per §3, so the status pills there are gone with them). Superseded 2026-08-26: the prototype's two link-tile galleries ("Director dashboards" / "School dashboards") duplicated the sidebar's own nav one-for-one; Home's job is now to show something FROM each dashboard, not a second way TO it — the sidebar remains the one menu (Main shell row, below). **Renamed 2026-08-31: "Home" is now "Dashboard".** The screen is the one overview a user lands on, and "Home" said where it sat in the nav rather than what it showed. The nav label and the topbar crumb are the only user-visible strings that moved; the route key, `/api/home` and `Home.tsx` keep their names, because `Dashboard*` already denotes the predefined reports throughout the orchestrator (`DASHBOARD_IDS`, `buildDashboard`) and renaming into that collision would cost more clarity than it bought. The plural "Dashboards" therefore stays the name of the predefined-report group and is never a label for this screen. **KPI strip amended 2026-08-31:** four summary cards, in this order — Students (gender mix), Staff on roll (permanent / not permanent / unclassified), Student attendance (present and absent student-days) and Total fees (collected / pending). Each leads with one figure and carries its parts beneath it (`kpi.breakdown`, 2–3 parts, pre-formatted server-side like the figure itself). Card order is the server's widget order and is contract, not presentation. **Amended 2026-09-01:** the first card no longer gets a hero (double-width) treatment — all four cards are one size, per §3. Two cards changed meaning rather than merely gaining a split — fees now leads with the year's DEMAND and shows arrears as a part (the bare outstanding figure could not be read without the size of the book it came from), and the staff split names an "Unclassified" part wherever `stafftype` holds one of the ERP's opaque codes rather than guessing it into permanent or not. **Grid amended 2026-08-31:** the card grid is curated and in a fixed server-side order — Fee Collection, Fee Defaulters, Fee by Student, Attendance, Staff Attendance, Enrollment, Staff Overview, Transport — sent as `grid` on `/api/home` rather than derived in the SPA. It opened at six and grew to eight as Staff Attendance and Fee by Student were built; every id on it must have a drill path and a drill query, held as `[MANDATORY]` in `test/home-previews.test.ts`, because a card drawing a chart nobody can click lies about what happens when you click it. Each card draws the report's DRILL-ENTRY chart (level 1, one bar per school, `DASHBOARD_DRILL_QUERY`) where a curated path exists, and its lead chart where it does not; every one of the six drills as of 2026-08-31, when Staff Overview (school→department→designation) and Transport (school→route→class) gained paths of their own. Every `available` dashboard used to get a card, which was nine and growing — the sidebar drawn larger. The "More dashboards" strip below carries built-but-not-gridded dashboards, all clickable and unpilled — it briefly also carried `coming` and `blocked` cards as inert pilled chips, until 2026-09-01 stopped serving those anywhere (§3). **Grid amended 2026-09-03 (third UI pass) — a card draws the chart that answers ITS question, and the grid is a bento.** Every card used to draw its report's DRILL-ENTRY chart, which was correct and redundant: level 1 of every curated path is "one bar per school", so eight cards drew the same three horizontal bars and only the axis told them apart. A page of one chart repeated is not an overview — there is nothing to compare when every card makes the same kind of statement. The server now names a chart per card in `DASHBOARD_PREVIEW` (`services/dashboards.ts`): Fee Collection draws receipts by month and Staff Attendance present staff-days by month (**lines** — the subject is movement over time), Attendance draws what was recorded and Enrollment the gender mix (**rings** — the subject is composition), Fee Defaulters draws overdue by age of the debt (a **bucket bar** — "how bad", not "which school"). Fee by Student, Staff Overview and Transport keep the drill entry, because outstanding per school, headcount per department and riders per route genuinely are their best reading. Every entry names ONE statement the report already vets and already runs, so a card still costs exactly one scan. **The cost is stated rather than hidden:** a card with a declared preview is INERT — its chart is not the drill entry, so clicking it opens the full report instead of descending in place. Nothing lies (a chart takes a click only when the widget says `drillable`), it is not a new state (Staff Overview and Transport rendered this way before they grew paths), and the drill itself is untouched on the report page at all three levels. The `[MANDATORY]` "every gridded id has a drill path" test survives the change because it was never really about the card: it is about what earns a place on the overview, and a report that cannot be descended belongs in the strip below. **The layout follows the charts.** `.pgallery` is a 12-column `dense` grid: a line takes **8**, a ring **4**, a bar **6**. The kind travels on `/api/home` `grid` (now `{id, kind}`, not a bare id) because each card is its own request and the slot has to be sized before the chart lands — sizing from the widget would lay eight equal skeletons and reflow the page under the reader one card at a time. `dense` is what makes it tile: the server's order is a RANKING (money first) and is not bent to suit arithmetic, so the spans as ranked — 8, 6, 6, 4, 8, 4, 6, 6 — pack into four full rows with the reading and tab order untouched. A **donut renders its key and its centre label at card size now**, where `compact` used to drop both: that was defensible while a ring reached a preview card by accident, and is not now that the grid CHOOSES rings — a ring with no key is four coloured arcs a reader cannot name, and the shares are the whole finding. **Rounded wedge caps and the padding gap are dropped for a ring that carries a slice under 1.5%**: neither is defined for a wedge narrower than itself, and what Recharts draws instead is a thin spike out of the ring that reads as a data point. Attendance is the case that forced it — NIWD, NA and Half Day all round to 0% and are all, correctly, still shown. **Grid amended again 2026-09-03 — a report may hold TWO cards, and the fee drills are back on the overview.** `DASHBOARD_GRID` was a list of report ids, one card each, which forced the two fee reports to choose between the two things worth seeing. Fee Collection's receipts curve answers "is the year on track"; its demand/collected/pending bars answer "which school is behind" — and that second chart is also the one a reader **drills**, school → quarter → class, in place on the card. On the two reports this product is opened for most, showing one and losing the other is the wrong trade. The grid is now a list of **slots** (`GridSlot`): `{key, report, chart?}`, where `chart: 'drill-entry'` pins a card to its report's level-1 chart instead of the chart `DASHBOARD_PREVIEW` names. Ten cards, of which Fee Collection and Fee Defaulters each hold two. A slot is not a new report and costs nothing beyond its own single scan — both fee slots name statements those reports already run for their own pages. The two cards of a report share a title, because they are two views of ONE report and both open it; what distinguishes them is the chart's own title beneath the head, which is the widget's, from the spec. `key` — not the report id — is what `/api/home/preview/{key}` takes and what the SPA keys its previews map by, since keying by report would make two cards of one report overwrite each other. `[MANDATORY]` in `test/home-previews.test.ts`: keys are unique, and a `drill-entry` slot names a report that really drills. Each card's accent is keyed by **card**, not report, because the two fee views are not the same subject — money coming in is not money still out. **Drill in place 2026-08-31:** a grid card drills where its report has a curated path (ADR-020) — clicking a bar replaces the card's chart with the next level and shows a compact trail (crumbs + ← Back; the level counter and ⟲ Reset are dropped at card size, and the level's NOTES are not). The card head (icon, title, →) opens the full report; the body stops propagation so a bar click drills instead of navigating away from the level it just asked for. A failed drill is reported inside its own card, never as a page banner. Page and card share one implementation (`components/Drill.tsx`) so the level arithmetic cannot drift between the two surfaces. |
| **Predefined dashboard** | Title + scope line; filter pills; KPI strip; charts; data table; footer: ⬇ PDF · ↺ Refresh · 🧠 View logic · ⧉ Clone & customize · 🤖 Ask AI about this data; drillable panels carry the "⌄ Drill-down · 3 levels" chip; a cloneable chart (docs/06 §3) carries its own small ⧉ in its panel header — a popover for name, academic year and, on a bucketable widget, week/month/quarter/year — separate from the page-level Clone button, which still clones the whole dashboard |
| **Module Wise Analysis** | Added 2026-09-01. Seven tiles — **Fees · Student · Staff · Attendance · Transport · Exam · General** (`services/modules.ts`, `MODULES`; 3-up, `.mgrid`). Each tile carries its icon, a one-line blurb and **the names of the reports inside it**, not a count: a count says how much is behind the tile, the names answer the question a reader actually has ("where do I find defaulters"). Order is a ranking and is the server's: money, then the two that describe the people in the building, then the operational two, then Exam, then General last as the "everything else" bucket. A module is an **arrangement of the predefined catalog** — it has no SQL, no serving path and no ADR of its own; membership is declared on the report card (`DashboardCard.modules`, `services/home.ts`) and travels on `/api/home` as `modules`, so the sidebar, the tiles and the cards read one answer. **A report may sit in more than one module** and does: Trend Analysis is in Fees and Student (six years of collection, twelve of enrollment), Staff Attendance in Staff and Attendance. Same report, two doors, never a copy. **An `empty` module renders, unclickable, with its reason** — Exam today, quoting its report card's own words ("No exam data exists in the ERP extract"). This is the one place a tile is served where a menu row would be withheld (§3), and the reason it is allowed: seven tiles describe a school's world, a hole in them reads as "exams were forgotten", and a tile that does not click promises no screen. The reason is printed **on the tile**, never only in a `title` — a tooltip is invisible to anyone who does not hover, including everyone reading a screenshot. |
| **Module (opened)** | ← Module Wise Analysis · icon + module title · blurb, report count, and how many cards are still loading. The module's reports as the **same live `PreviewCard`** the Dashboard grid draws (`.pgallery`, 2-up) — same chart, same drill, same accent, because a module is an arrangement of the catalog and a card that behaved differently in here would read as a different product. A page of names with arrows was the alternative and was rejected: "all the fee-related analysis" is a promise about *analysis*, and a menu is not one. Cards are fetched **when the module is opened**, not with Home — every served dashboard is in some module, so warming all of them up front would fetch the whole catalog on launch to draw seven tiles — and ids already fetched for the Dashboard grid are reused rather than re-queried (one `previews` map for both screens, `App.tsx`). ← Back from a report opened here returns **to the module**, not to Dashboard, and the 🧩 row stays lit the whole way down. **This is what widened `buildHomePreview` from the curated grid to every *served* dashboard** (2026-09-01): Admissions Funnel, Library & Textbooks and the Principal's Snapshot are reports a reader can already open in full, and two kinds of card inside one module — live charts for the gridded ones, dead tiles for the rest — would have been a distinction no reader could see. The GRID is untouched: still the curated eight, still ranked by the server, still holding every card on it to a drill path. A *withheld* dashboard is still refused a preview, because there is no report to draw. |
| **Drill view states** | Pointer cursor + hover hint → in-place chart swap → breadcrumb `Report ▸ Apr-26 ▸ Class 9` (clickable crumbs) + ← Back + ⟲ Reset + "level n of 3" + recomputed slice total; deepest-level note |
| **Ask AI** | Split: chat (35%) with streaming status steps, suggestion chips, Refine · artifact canvas (65%) with KPI chips, chart, table, narrative, and actions ⬇ PDF · 💾 Save · 🧠 Logic · ⧉ Clone · ✎ Refine; mobile: two swipeable tabs. Locked state: centered card with 🔒, "predefined dashboards already available", setup CTA |
| **Report editor** | Renamable title; **Report type: Simple / ⌄ Drill-down (3 levels)**; visual logic editor (source, filter chips, group-by/levels, chart per level) beside the **live-regenerating SQL** panel; ✨ Modify with AI; interactive preview (clickable through levels for drill reports); Save as my report |
| **Custom report view** | Report + version + "cloned from"; 🧠 View logic panel; Edit / Clone / PDF; each chart carries its own **✦ Ask AI** button (owner-only; locked, not hidden, when AI isn't set up) opening a side panel — chat about that chart or ask for a change, with **Apply as a new version** to persist a proposal (docs/06 §1) |
| **My Reports** | Custom reports (CUSTOM badge, version, View/Edit/Clone/PDF) + AI snapshots (AI badge, Re-run/PDF); ＋ New custom report |
| **Agents home** | Fleet KPIs; agent rows (status dot, schedule, channel icons, last run, ON/OFF switch, Edit flow, Runs); template gallery |
| **Agent builder** | 3-panel: palette | canvas | properties; toolbar ▶ Test run · 💾 Save draft · 🚀 Publish; lint badges; live counts after test; 🤖 "Describe your workflow" drawer |
| **Test-run panel** | Dry-run: node counts, exact rendered message previews per branch, "send test to my phone" |
| **Runs & history** | Run list · flowchart replay with per-node status/payload · Retry on failures · CSV export |
| **Settings — AI (org)** | 3-step BYOK wizard (Console guide + PDF, key + model + monthly cap, Test & Save, verification); post-activation: status, per-school usage/cost meter, Replace/Disable; error banner path |
| **Settings — Messaging channels (school)** | Email(SMTP) / SMS(DLT) / WhatsApp(BSP) provider rows with Connected status + Connect/Disconnect; Template Manager note (approved templates only) |

## 3. UX conventions (bind all screens)

- **Locked ≠ hidden, but a menu row is a promise (amended 2026-09-01):** a *gated* feature stays visible with a lock and a path to unlock — hiding it would hide the setting that opens it, and 🔒 → Settings drives setup better than hiding does. Ask AI is that case and, in the menu, now the only one. The rule does **not** extend to a feature nobody can open under any role, with any key, from any screen: those are withheld entirely. Concretely, `/api/home` serves only `status: 'available'` cards (`servedDashboards`, `apps/orchestrator/src/services/home.ts`), so both surfaces that list dashboards — the sidebar and Home's "More dashboards" strip — drop the other two states together. That covers `coming` (serving path unbuilt: Group Overview, Cross-School Attendance, School Comparison on the rollup store, ADR-010; Workflow Agents on the agent runtime, ADR-022) and `blocked` (the ERP extract holds no such data, AUDIT_REPORT C20: Exam Performance today), plus the sidebar's hand-written "Agents · SOON" row. The cards keep their entries, reasons and ADR references in the catalog, which is where the people who can act on them read them — and a card returns to the menu by turning `available`, with nothing to re-add by hand. **KPI tiles are unchanged:** `blocked_metrics` still renders "— · no data" on Home, because a tile explains a number the strip is visibly missing, where a menu row promises a screen that does not exist. **Module tiles take the same carve-out (added 2026-09-01).** `servedModules` serves a module whose every report is withheld as `status: 'empty'`, with the withheld reports' own reasons on it, and the tile does not click. The seven modules are a fixed description of a school's world rather than a list of destinations — drop Exam and six tiles assert that schools have no exams; keep it and it states that the ERP sends no exam data, which is a fact only the extract's owner can change and only this tile can tell a school. The line that does not move: the tile quotes the CARD's reason and never writes one, so an arriving extract fills the tile in by flipping one `status`, exactly as it fills the menu.
- **A mark has to read as a mark (added 2026-09-01).** A bar is drawn thick enough to be a shape rather than a rule: **22px** for a single horizontal series, **16px** for one of several sharing a band, **38 / 26px** for the vertical equivalents (`BAR_PX`, `packages/chart-spec/src/react/widgets.tsx`), with a **4px rounded data-end and a square foot at the baseline** — rounded at both ends would float the bar off the axis it is measured from. These are caps, not widths: a chart with many categories still thins out on its own, and a band is budgeted at roughly bar-plus-half-a-bar so the leftover stays air. Past ~24px a bar stops being a mark and becomes a block, which is why the ceiling is where it is. The Dashboard preview grid was **2-up** (`.pgallery`) for the same reason: it went 4-5 → 3 → 2 columns, and each step was the same complaint — the chart inside the card was too small to read as a chart, and the preview IS the reason to click the card. **Amended 2026-09-03:** it is a 12-column bento (8 for a line, 6 for a bar, 4 for a ring — Dashboard/overview row above). The floor that argument established is intact and is what the spans respect: the narrowest slot the grid can produce is 4/12 of a ~1650px page, and it is only ever given to a ring, which is a fixed-size object with a key beside it and gains nothing from half a page. What the old rule missed is that not every chart wants the same width — a twelve-month trend is read left to right and gets better the longer it is, where a ring given 900px just sits in the middle of a lake of white. Below 1400px the bento collapses back to two equal halves, and below 980px to one card per row. A compact card's chart stretches to match a taller neighbour in its row but **no further than 1.3× its natural height**, so a 3-school chart sharing a row with an 8-department one keeps its rows together instead of drifting apart.
- **Scope is always on screen:** picker chip in the topbar; scope line under report titles; scope printed on PDFs.
- **Transparency affordances everywhere:** 🧠 View logic and ⧉ Clone appear on every report surface, including AI artifacts.
- **Status streaming for anything slow:** AI steps, agent test runs, PDF generation — never a bare spinner.
- **Destructive/irreversible actions** (publish agent, disable AI, disconnect channel) confirm consequences in plain language, and every such state has a reverse path.
- **Empty states teach:** My Reports and Agents explain how their first item is created.
- **Two-up is the default rhythm; full width is earned from the data (added 2026-09-03, second UI pass).** Amends the footprint rule below. Every panel variant is now **span 6** except `wide`, which stays 12. `hero` (7) and `side` (5) existed to pair one line chart with one donut, and that single pairing bought an off-grid rhythm the rest of the page had to work around — a 7 followed by anything but a 5 stranded columns, and `dense` papered over it by pulling later panels out of the server's order. Equal halves pair with any neighbour. **What earns a full row is the widget's DATA, not its kind**, and which part of the data depends on which way its categories run (`needsFullWidth`, `packages/chart-spec/src/react/widgets.tsx`): on a **horizontal** bar the categories run down the panel, so their number sets the height and only label length competes for width (`longest > 16`) — fifteen classes labelled "NURSERY" read fine in half a page where five departments labelled "ADHOC OFFICE STAFF(A.O.S)" do not; on a **vertical** axis, and on a line's periods, every category needs its own slice of width, so count counts too (`longest > 16 || count > 14`) — 78 months in half a page is a smear. A **table** earns 12 by carrying more than four columns: eleven columns of figures cannot be read in half a page, three columns of short strings were taking a full row for nothing. **A drill panel is now 6 at every level, where it was 12.** The stability requirement of the rule below is unchanged and is still the point — a panel that resizes as the reader descends is not "in place" — but 12 was only ever the span both orientations could share back when horizontal meant 12 and vertical meant 6. With both now 6 unless their data earns more, 6 is equally stable and costs half the screen; and the span is fixed for the whole descent rather than re-derived per level, which would reintroduce exactly the resizing being avoided. The driver is screen space: on a 1900px page a full-width panel holding three bars spends about a thousand horizontal pixels on nothing, and a reader comparing two panels scrolls between them instead of putting them side by side, which is most of what a reporting surface is for.
- **Density is the house style, and the first UI pass overshot it (added 2026-09-03).** That pass made the charts quieter and then spent the quiet on air — radius 12→14, panel padding 20/22, grid gap 16→20, KPI figure 26→29, table rows 9→12 — on the theory that a quiet card takes its definition from whitespace. On a page of eleven panels what it actually bought was a longer page with less on each screen, which is the generic SaaS admin look rather than an analytics product. Corrected: **card radius 8** (what §1 said all along; the 14 was drift), **shadows reduced to a hairline** because a panel is delimited by its 1px border and the canvas behind it and a shadow on top of a border says the same thing twice, **grid gap 12**, **panel padding 12/14**, **panel title 13px**, **KPI figure 24px on 11/12px padding**, **table rows 7px at 12.5px**. Card entrances fade rather than rise, and a KPI tile no longer lifts on hover: a tile is not a control, so movement under the pointer promises an interaction that does not exist. A preview card still responds, because clicking it does open the report — with a border, not a 3px rise. The rule these follow: **frame is not content.** Every pixel of padding, radius, shadow and gap is a pixel the charts, KPIs and tables do not get, and they are the reason the page exists.
- **A panel's footprint comes from the widget, never from the report (added 2026-08-31).** Report bodies are a 12-column dense grid (`.specPanels`). The renderer, not the spec, decides each panel's span — a vertical bar is 6, a table or a horizontal bar is 12, and a line is 7 so that a donut's 5 completes its row (Fee Collection's "Receipts by month" beside "Payment modes" is the reference pair). A spec that could pin its own width would let a saved report outlive a layout change, exactly as a spec that could pin a colour would outlive a palette audit — so it cannot. The rule follows from the widget's own SHAPE, the same way a bar's orientation follows from its label lengths: a **multi-series** line is 12, because it carries a legend and twice the marks, the reader is matching two shapes point by point, and a report comparing two years has no five-slice breakdown to pair with — at 7 it strands 5 columns that no later panel can fill, since every remaining panel is 12. A **drill panel** (any level of an ADR-020 path) is 12 at every level regardless of orientation, because a path changes its categories as it descends — Comparative Analysis runs "World School" → "April 2026-27" → "NURSERY", so orientation alone would size it 12, 12, then 6, halving the panel under the reader on the last click and freeing six columns nothing below can fill. docs/06 §4.4 has a drill *swap the chart in place*, and a panel that resizes as you descend is not in place: the page reflows under the cursor that just clicked, and ← Back reflows it again. A stable frame is what makes three levels read as one chart being narrowed rather than three charts. **The KPI strip is the exception, amended 2026-09-01: every tile is the same size, at every count.** The first tile used to render double-width at a 36px figure, capped at five tiles because a 2× lead broke a six-tile strip into 5 + 1 — and that cap was the rule arguing against itself, since emphasis you have to switch off as the strip grows is a layout constraint dressed as a reading order. On Dashboard's four cards it read as an accident before it read as emphasis: one wide tile beside three narrow ones sends the eye to the odd shape rather than the number in it. A strip ranks by **order** (the server's `spec.widgets`, contract) and signals by **tone** (the left-edge colour, from meaning the server assigned); neither needs a tile to change size, and both survive the strip growing by a card. `.kpi` is now one rule — `flex: 1 1 190px` on every tile, no per-tile override — so four tiles divide the row in four and six divide it in six. Paper agrees (print.css's fixed 4-column grid); the unavailable-metric tile agrees too, its figure raised 24px → 26px to match a live one, since what marks it unavailable is its muted colour and `NO DATA` pill.

## 4. Assumptions

Desktop-first (admin work); layouts degrade to single-column at ~980 px; the Ask-AI split becomes tabs on mobile. Localization (Hindi/regional) is anticipated in message templates (AI-compose language option) before full UI localization.

## 5. Reference artifacts

A clickable HTML prototype implements every screen above with sample data ("Sunrise Trust": Delhi/Noida/Gurgaon) including the ERP login → launch handoff, drill-down, clone/editor, agents and BYOK gating; a 19-slide architecture deck mirrors the same design system. Both are **design references, not production code** — the design system tokens and conventions in this document are the binding spec.
